#include "kt_mempool.h"

struct kt_mempool *kt_mempool_create(struct kt_allocator *al, char *name, i32 esize, i32 count)
{


    // return pool;
    assert(0 && "kt_mempool_create not implemented");
    return NULL;
}